# Go Operators

Go operators are special symbols that used to perform calculations, comparisons and logical operations on variable.

#### Examples of Go operators are:

+,-,*,/,%,=, =!, ==,&&,<, >, <=, >=, ||, ++, -- e.t.c.