# Go struct
A struct short form of structure, is used to create a collection of members of different data types, into a single variable.

### for example:
ype User struct {
    FirstName string
    Age       int
    Active    bool
}
